# 研究内容二最小原型验收报告

## 验收环境与范围

本报告只验收内存状态原型。它不模拟QBFT共识，不作为联盟链性能证据。

```text
Python：3.13.11
pytest：61项通过，0失败
分支覆盖率：92.58%
Hypothesis：1500例
Baseline-I/Proposed-C随机语义一致性：1000例，无反例
CAP1随机往返：500例，无错误
攻击测试：两方案均执行，非法接受0
并发重放：每方案50次并发，仅1次成功消费
```

## 门槛

```text
Baseline-I与Proposed-C合法授权结果不一致数 = 0
攻击接受数 = 0
合法请求误拒绝数 = 0
Nonce并发重复成功数 = 0
状态机不变量违反数 = 0
规范编码往返错误数 = 0
```

## 五项核查

1. Proposed-C是否实际消费`C(P)`：是，祖先路径命中并绑定节点和Cover版本。
2. 两方案区别是否只位于冻结执行步骤：是，公共Issuer/Verifier未复制。
3. 是否共享全部安全检查：是。
4. 是否故意弱化Baseline-I：否，使用二分Matcher与二分TTL区间定位。
5. 是否足以设计后续对照实验：是，但尚不足以宣称Proposed-C有优势。

## 退出判断

```text
Baseline-I是否完整实现：是
Proposed-C是否完整实现：是
C(P)是否被协议真实消费：是
两种方案授权语义是否一致：是（在形式化设计和现有测试范围内）
攻击测试是否全部通过：是
公平比较条件是否冻结：是
降级规则是否冻结：是
重大设计问题是否全部关闭：是

是否允许进入Besu联盟链实现：是（仅允许架构设计与受控实现）
```

正式联盟链性能采集仍需单独的配置冻结、预实验和准入验收。

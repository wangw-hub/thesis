# Statistical Design Review

All four methods reuse the same frozen slot sequence per workload, seed, and
repetition. Confirmatory inference is a 10,000-resample run-level paired
bootstrap. Request rows are descriptive observations, not independent
inferential replicates. Verdict: `PASS_PENDING_DRY_RUN`.

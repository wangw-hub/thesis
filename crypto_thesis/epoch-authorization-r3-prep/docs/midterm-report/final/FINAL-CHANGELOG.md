# FINAL-CHANGELOG（M7 → 最终固化版）

- 基线：`docs/midterm-report/m7/M7-MIDTERM-SOURCE.md`（36 页）
- 产出：`docs/midterm-report/final/output/王威-专业学位研究生学位论文中期考评表-最终固化版.docx/.pdf`（37 页）

## 1. 参考文献（34 篇，未增减）

- 删除全部访问日期：Besu、PostgreSQL、IPFS 的 `[2026-08-02]`；IPFS `2014[2026-08-02]` → `2014`。
- [25] BE-DSN 补 article number：`Cluster Computing, 2025, 28(7): 437`（dblp/Springer/EBSCO 核验）。
- [1-4] 引用语义：改为“传统角色/属性访问控制及密码学访问控制机制[1-4]”。
- [34] IPFS 锚点移至“Kubo 以内容寻址标识关联副本对象[34]”；SHA-256 完整性权威改为本文设计说明。

## 2. 公式（16，未增减）

- 式(10)：单行双定义 → OMML 对齐数组（eqArr）单编号，PDF 不折行。
- 式(11)：正文补充 enc/ct 语义、实参顺序与冻结实现 `seal_base(pk_R, CK, info, aad)` 一致、错误 Info/AAD 测试证据。
- 式(15)/式(16)：维持必要条件/候选可接受边界，正文无过强表述。

## 3. 算法（8）

- 算法2：块大小选择完全形式化（pos=0 → 2^⌊log2 remaining⌋；pos>0 → lowbit(pos) 收缩）；U 标注为校验参数。
- 算法4：双读语义修正（两次读取最新确认块，状态不一致拒绝；删除“同一确认区块”）。
- 算法6：补充合法接收者集合重建与 HPKE 封装重生成（I6 证据：revokedRecipientAbsent/legalRecipientRetained/bodyDigestUnchanged）。
- 算法8：输出“一致对象或恢复判定（RecoveryDisposition）”，区分缺失/损坏/可信备份/不可恢复/人工核对。

## 4. 符号

- RC2 用户五元组 U → U_u；RC1 冗余度 R → R_d；RC2 能力待签字节 B → B_cap；全局冲突 0。

## 5. 主张与措辞

- OAuth/JWT 限定化；许可联盟链术语统一；绝对化安全表述删除；问题排序逻辑修正；学位论文/小论文区分。

## 6. 图表

- 图12：0–100% 全尺度 + 96–100% 内嵌放大；
- 图17：离散点图（无连接折线）；
- 图20：修正 E5 配置映射（C1/C5/C2/C6），标题“可比较故障场景下的恢复端到端时延”，图注说明 Fail-Closed 排除；
- 表7：4 故障 × 2 来源 = 8 行，冻结 E5 中位数，表题注明 Fail-Closed 时长语义，整表单页。

## 7. 内容精简

- 研究内容三数据库控制面：3 段重复合并为 2 段，保留 job commit/独立可见性/准入/回执/固定块/操作标识幂等/COMMIT_UNKNOWN/不跨回执等待。
- 整体闭环段：闭合句精简，保留“不是三项独立工作的拼凑”论证与三个接口关系。

## 8. 阶段成果

- 论文：状态改为“正在形成论文稿，拟投稿《软件学报》”（无同名初稿文件）；两篇本地草稿标题不同，已标记需作者确认。
- 专利 1/2：拟申请发明专利，专利文本撰写中；无虚假申请状态。

## 9. 未修改

- I9–I12 raw 数据、冻结实验数值、三项研究主线、负结果、论文题目、系统结构图（未重绘）；未 push。

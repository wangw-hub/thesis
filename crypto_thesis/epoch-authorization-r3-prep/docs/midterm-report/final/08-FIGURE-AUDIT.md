# 08 图表审计（FIGURE AUDIT）

- BROKEN_FIGURE = 0；MISLEADING_FIGURE = 0；SYSTEM_FIGURE_REDRAW_COUNT = 0
- 图 1–20 图号连续、图题完整；系统图（图1/2/3/7/8/15/16）继续使用本地 GPT 已确认图片，仅版式调整。

## 图 12 阶段占比（科学表达修正）

- M7：纵轴 96–100 截断，视觉放大 98.66% 与 98.80% 的微小差异。
- FINAL：主图 0–100% 全尺度堆叠柱状图 + 96–100% 内嵌局部放大；数据不变。

## 图 17 生命周期路径（离散类别）

- M7：折线连接四类生命周期路径，暗示连续趋势。
- FINAL：点图 + Bootstrap 95% CI 误差棒，无连接折线；四类独立类别（初始发布/密文主体与密钥轮换/撤销闭合/副本恢复）。

## 图 20 恢复时延（标题与数据范围一致）

- M7：标题“故障恢复端到端时延对比（对象来源×故障场景）”，且 E5-C1..C4 配置映射错误（将 CID 不一致/双端缺失数据标为对象损坏）。
- FINAL：标题改为“可比较故障场景下的恢复端到端时延”；柱序修正为 E5-C1（仅本地无故障）、E5-C5（隔离副本无故障）、E5-C2（仅本地对象损坏）、E5-C6（隔离副本对象损坏）；图注注明“内容标识不一致与双端缺失场景按 Fail-Closed 终止，不纳入恢复时延比较”。
- 未编造失败运行时延；Fail-Closed 场景时长仅出现在表 7 并注明语义。

## 数据一致性

- 图 12 数据源：RC2 `figure-5-7-stage-share.csv`（未改）。
- 图 17/20 数据源：RC3 `descriptive-statistics.json` / `bootstrap-results.json`（未改，仅修正配置映射与图形语义）。

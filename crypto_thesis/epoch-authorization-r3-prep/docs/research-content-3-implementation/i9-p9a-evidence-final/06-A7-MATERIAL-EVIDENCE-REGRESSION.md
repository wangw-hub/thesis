# A7 material evidence regression

Unit and serialization regressions pass for identical outer/scenario/final/strict projections, `ALLOWED_AFTER_CURRENT_HEADER_ONLY`, and failure-before-evaluation `NOT_EVALUATED`.

The requested new real `EpochAdvanced` development run was **not executed**. Remote admission passed, but exporting the isolated-chain account credential file was rejected pending explicit authorization. No substitute or fabricated result was used.

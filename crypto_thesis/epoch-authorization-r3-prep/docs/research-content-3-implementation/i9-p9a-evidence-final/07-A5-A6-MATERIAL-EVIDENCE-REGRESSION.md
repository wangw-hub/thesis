# A5/A6 regression

A5 evidence retains ordered observations `DENIED/RECOVERY_IN_PROGRESS` then `ALLOWED/RECOVERY_COMPLETED`. A6 retains `DENIED/HEADER_UPDATE_PENDING` and remains a valid `FAIL_CLOSED_EXPECTED` run in dry-run acceptance. Both contract regressions pass.

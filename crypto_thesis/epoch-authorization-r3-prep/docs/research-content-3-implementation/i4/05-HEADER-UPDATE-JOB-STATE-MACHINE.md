# Header更新任务状态机

状态冻结为PENDING、CLAIMED、CANDIDATE_STORED、READY_FOR_CHAIN_COMMIT、
COMMIT_UNKNOWN、COMMITTED、RETRY_WAIT、FAILED_TERMINAL和DEAD_LETTER。
数据库触发器和Repository共同拒绝非法转换、COMMITTED回退、DEAD_LETTER恢复及
row_version非单步递增。COMMITTED必须有`CONFIRMED_TEST_DOUBLE` CommitAttempt。


# HPKE_PROVIDER_V1 决定

冻结 `pyhpke==0.6.4` 为 `HPKE_PROVIDER_V1`，Suite 为 Base(0) / DHKEM(X25519, HKDF-SHA256)(0x0020) / HKDF-SHA256(0x0001) / AES-128-GCM(0x0001)。许可证 MIT，Python 3.13.11 验证通过。仅使用公开 API，未修改第三方源码，未手工实现 HPKE。

# Canary phase contract V1

The scenario is `CANARY_INITIAL_END_TO_END`. Every known phase is in exactly one of `required`, `optional`, `notApplicable`, or `forbidden`. Not-applicable phases are declared in the contract and must be absent from execution JSONL; they are not missing phases.

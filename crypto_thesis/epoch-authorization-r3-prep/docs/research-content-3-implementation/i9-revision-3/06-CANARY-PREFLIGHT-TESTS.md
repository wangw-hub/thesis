# Canary preflight tests

Local preflight: 31 passed, 1 isolated-chain-only test skipped. It covers strict decoder shape/type checks, phase partitioning, required events, N/A semantics, event ordering, and immutable attempt behavior. The isolated-chain read is separately required before the Canary.

# Revision-3 Canary result

`NOT_STARTED`. The required isolated-chain CompositeState preflight failed closed with `ExtraDataLengthError` while fetching the Besu block; no transaction, database operation, body, header, or evidence run was created.

# Revision-3 attempt identity

No new attempt identity or run identifier was created because the mandatory isolated-chain read-only preflight failed. This avoids any ambiguous or silently retried Canary.

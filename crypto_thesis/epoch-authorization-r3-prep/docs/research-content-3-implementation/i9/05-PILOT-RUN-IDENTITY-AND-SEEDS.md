# Run identity and seeds

`runId = SHA-256("EPOCH_AUTH_R3_I9_PILOT_RUN_V1" || canonical identity config)`. Display timestamps are excluded. Workload seeds are explicit; CK and nonce material use secure randomness.

# P9-D Strict Result

The frozen 12-scenario × 2-seed matrix completed 24/24 valid runs.  Every
record contains `FaultInjectionEvidenceV1` with distinct injection and
observation evidence.  The isolated Besu and Kubo service scenarios were
stopped, independently observed unavailable, restarted, and readiness-checked;
formal services and validators were not accessed.

Gate: `P9_D_PASSED`.


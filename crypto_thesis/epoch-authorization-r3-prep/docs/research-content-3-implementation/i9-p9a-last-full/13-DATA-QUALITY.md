# Data quality

Runner reported planned/actual/valid=8/8/8 and raw SHA errors=0. Independent strict review marks accepted valid runs=0 and unexpected invalid runs=8 because every raw omits the mandatory attempt-scope classification. This was not repaired after sealing.

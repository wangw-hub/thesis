# I2 范围与非目标

I2 仅实现本地内容寻址、不可变对象、原子可见、写后/读取验证、幂等、并发与故障检测。它不提供对象机密性、授权、网络访问控制、多副本、持久可用性、root 防护、IPFS 分发、数据库事务或链上锚定。

Body 机密性来自 I1 BodyFormatV1 与 CK；本地 SHA-256 完整性不能替代链上 Header 锚点。本阶段不实现 Versioned Header，不产生性能结论。

# Strict review

FATAL=0, MAJOR=1, MINOR=0. The remaining MAJOR is PostgreSQL `application_name` length normalization: the configured identity must be bounded to 63 bytes and attested against the exact transmitted value before another Canary can be admitted.

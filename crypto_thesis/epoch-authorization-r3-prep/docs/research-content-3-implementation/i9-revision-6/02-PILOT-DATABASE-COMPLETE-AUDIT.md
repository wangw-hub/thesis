# Pilot database audit

Runtime attestation confirmed `127.0.0.1:55432`, database/user `epoch_auth_r3_i9_pilot`, PostgreSQL 16.14, and zero fallback attempts. Revision 4's single historical 5432 targeting defect remains separate. The Canary later failed because PostgreSQL truncated an overlong `application_name`, not because it reached another cluster.

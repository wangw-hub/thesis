# Bootstrap state machine

The accepted linear path is `PLANNED → STAGING_CREATED → CONFIG_WRITTEN → CONFIG_VALIDATED → DATABASE_ATTESTED → WEB3_ATTESTED → PREFLIGHT_COMPLETED → READY_FOR_CANARY → PUBLISHED`. Skips fail; `BOOTSTRAP_FAILED` is terminal.

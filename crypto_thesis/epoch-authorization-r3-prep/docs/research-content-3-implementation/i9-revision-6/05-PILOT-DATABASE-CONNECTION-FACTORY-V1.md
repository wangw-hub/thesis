# PilotDatabaseConnectionFactoryV1

The factory loads the external password, passes only explicit connection parameters, and attests database, user, server address/port, version, and application name before yielding a connection. It has no retry or alternate-port path.

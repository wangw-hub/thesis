# PilotDatabaseConfigV1

Strict configuration requires explicit loopback host, integer port 55432, exact pilot database/user, application prefix, external credential source, PostgreSQL major 16, and cluster identity `16/r3_i4`. Missing, unknown, or fallback values are rejected.

# Storage replica authority amendment

Local `ObjectReferenceV1` and full SHA-256 remain authoritative. CID is a transport/replica locator only. A replica is usable only after exact-byte, length, SHA-256, and object-format verification.

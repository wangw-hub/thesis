# Failure and attack matrix

Invalid CID, wrong kind, wrong digest, cross-object mapping, truncated/corrupt data, missing Kubo, and corrupt local objects all fail closed. Root/host compromise and deletion by the storage administrator remain accepted infrastructure limitations.

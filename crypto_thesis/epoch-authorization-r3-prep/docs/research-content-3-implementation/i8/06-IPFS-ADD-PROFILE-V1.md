# IPFS Add Profile V1

CIDv1, SHA2-256 multihash, raw leaves, fixed 262144-byte chunker, no directory wrapping, pin enabled. Small single-leaf objects use raw codec `0x55`; chunked roots may use dag-pb `0x70`.

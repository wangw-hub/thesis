# StorageReplicaRecordV1

The record binds object kind, full object SHA-256 and size to backend, CID metadata, add profile, pin state, verification state, and Kubo node ID. `PINNED` requires `OBJECT_VERIFIED`.

# P9-A invariant report

Database and chain evidence report zero recorded invariant violations. Factory bypass, 5432 fallback, duplicate Anchor, duplicate COMMITTED and erroneous material release counts are zero. A6 nevertheless violates its approved scenario expectation: after three status-1 receipts the required fixed-block CompositeState could not be confirmed. The run remained fail-closed, left database finalization not reached, released no material, and was sealed before stopping the matrix.

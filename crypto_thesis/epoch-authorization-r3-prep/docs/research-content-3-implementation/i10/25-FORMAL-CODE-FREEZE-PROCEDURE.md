# Formal Code Freeze Procedure

Before any future execution: record Git SHA, dependency-lock digest, contract bytecode digest, minimal snapshot manifest, generator digest, analysis-code digest, and environment fingerprint; run local synthetic tests; obtain I11 approval; then create a new Formal attempt. Any code fix freezes the failed attempt and requires a new SHA, snapshot, and approval gate. I10 creates no attempt.

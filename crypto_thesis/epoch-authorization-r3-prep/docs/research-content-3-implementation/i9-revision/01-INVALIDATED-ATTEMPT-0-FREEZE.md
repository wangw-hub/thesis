# Invalidated attempt 0 freeze

The 93 original run directories are classified:

- `INVALIDATED_I9_ATTEMPT_0`
- `INVALID_REASON_PIPELINE_EXECUTION_DEFECT`
- `NOT_FOR_FORMAL_RESULTS`
- `NOT_FOR_PILOT_FACTOR_SELECTION`

The freeze audit recomputed 1,023 file hashes from the per-run manifests:
93 runs checked, zero SHA-256 errors.  No original config, phase event, state,
log, or hash file is rewritten by this revision.

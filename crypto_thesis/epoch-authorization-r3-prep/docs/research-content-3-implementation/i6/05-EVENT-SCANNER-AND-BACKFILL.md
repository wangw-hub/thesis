# Event Scanner and Backfill

`AuthorizationEventScanner.backfill_once(start,end)` is finite and validates chain identity through its caller. The real isolated scan observed four recent events, inserted four, and the repeat scan inserted zero with four duplicates. No unbounded loop is implemented.

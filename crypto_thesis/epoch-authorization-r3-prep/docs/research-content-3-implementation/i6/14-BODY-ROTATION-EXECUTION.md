# BODY_ROTATION Execution

The real isolated-chain transaction committed Header version 5 with Body/key version 3 and a new Body digest. A new CK encrypted a new BodyFormatV1 object. The old CK failed to decrypt the new Body. The new CK is persisted only as an encrypted 48-byte AEAD record.

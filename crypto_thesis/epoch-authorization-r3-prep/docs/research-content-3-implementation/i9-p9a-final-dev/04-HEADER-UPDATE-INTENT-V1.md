# HeaderUpdateIntentV1

The immutable intent carries schemaVersion, resourceId, event identity/type, updateKind, targetEpoch, targetStateVersion, authorization block number/hash and source transaction/log identity. Serialization round-trip is tested. No target-version defaults exist.

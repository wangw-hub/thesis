# A7ExecutionPathDiffV1

The previous DEV path called a wrapper that explicitly forwarded `PlannedResourceUpdate.target_epoch/target_state_version`; Final called `_anchor` without those keyword arguments and inherited defaults 1/1. Scanner, normalized event, resolver and RevocationAgent had already produced the correct 2/2. Both paths now import the same `header_update_intent_v1` and `build_header_only_anchor_from_intent` implementation. Execution-mode differences are limited to identity, evidence root, metadata and development labels; duplicate A7 business implementations: 0.

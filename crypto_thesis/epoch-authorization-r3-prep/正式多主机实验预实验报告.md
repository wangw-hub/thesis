# 正式多主机实验预实验报告

## 已完成

复用冻结 E1 生成器生成 81 个工作负载定义，覆盖 3 个 `U`、3 个碎片率、3 个局部性和 3 个并发度，全部标记 `PILOT_ONLY`、`formal_result=false`。四方案、公平缓存和环境守门接口已建立。

## 未执行

未执行多主机请求、PostgreSQL 共享 Nonce、角色分离部署、跨主机计时、指标采集和故障预实验。原因是当前无至少 4 个独立 OS 实例，也无可用远程 inventory 和 PostgreSQL 服务。守门脚本按设计拒绝运行。

本文件不包含性能结果，生成的 workload JSONL 不得进入论文主表或主图。
